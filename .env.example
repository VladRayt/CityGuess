# Copy to .env and fill in. All keys here are public client-side keys (bundled into the app).
EXPO_PUBLIC_APPSFLYER_DEV_KEY=
EXPO_PUBLIC_AMPLITUDE_API_KEY=
EXPO_PUBLIC_AMPLITUDE_SERVER_ZONE=EU

# Content pipeline only (never shipped in the app, not in EAS)
MAPILLARY_ACCESS_TOKEN=
EXPO_PUBLIC_ADMOB_REWARDED_ANDROID=
EXPO_PUBLIC_ADMOB_REWARDED_IOS=
